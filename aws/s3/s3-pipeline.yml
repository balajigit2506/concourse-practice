---
platform: linux

image_resource:
  type: docker-image
  source: {repository: busybox}

run:
  path: echo
  args: [I learnt how to set concourse pipeline using git as a resource and trigger the pipeline on commit..]
