 - task: run
    config:
      platform: linux
      image_resource:
        type: docker-image
        source:
          repository: bgdocker256/awscli-py
      run:
        path: echo
        args: [Running from a AWS CLI docker image...]
     
