---
platform: linux

image_resource:
  type: docker-image
  source: {repository: busybox}

run:
  path: echo
  args: [I am yet to learn how to automate ec2 instance provisioning but i learn to trigger the pipeline on commit..]
